# Copy to `.env` or `.env.local` (gitignored) — do not put real URLs only in vite.config.

# Vite dev server & preview port
VITE_PORT=5173

# Backend base URL for `/api` proxy and SignalR dev origin (include trailing slash)
VITE_PROXY_TARGET=http://localhost:5000/
